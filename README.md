# compromise-tiles

Pre-cut city data for the sunny-terrace filter of the Compromise iOS app:
building footprints with heights, terrace permits and OpenStreetMap
outdoor-seating venues, and the commune, one JSON file per ~200 m cell in
the app's own cache format (`tiles/<folder>/<int(lat*500)>,<int(lon*500)>.json`).
Generated by `tools/tiles/make_tiles.py` in the Compromise repository from
the city centres listed in `cities.json`, on a schedule.

## Licences and credits

- Building footprints in France: IGN BD TOPO, Licence Ouverte 2.0.
- Terrace permits: Ville de Paris (ODbL), Mairie de Toulouse and
  Eurométropole de Strasbourg (Licence Ouverte 2.0), Ville d'Anglet
  (Licence Ouverte 2.0).
- Everything from OpenStreetMap: © OpenStreetMap contributors, ODbL.
  Because these tiles include OpenStreetMap data, the tile set is
  published under the ODbL (share-alike).
- Communes: Etalab geo API, Licence Ouverte 2.0.

No personal data: every key is the rounded coordinate of a public area.
